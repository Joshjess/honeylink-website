---
phase: 04-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - DEPL-01
  - DEPL-05

must_haves:
  truths:
    - "https://honeylink.nl serves the SvelteKit site with a valid TLS certificate"
    - "The Node.js app auto-restarts after a crash via Docker restart policy"
    - "All 36 existing URLs from the Framer site resolve correctly (no 404s)"
    - "DNS points to Hetzner VPS IP (not Framer)"
  artifacts: []
  key_links:
    - from: "DNS A record"
      to: "Hetzner VPS IP"
      via: "Domain registrar"
      pattern: "A record pointing to VPS"
    - from: "Coolify"
      to: "docker-compose.prod.yml"
      via: "Git push trigger"
      pattern: "Auto-deploy on push"
---

<objective>
Provision the Hetzner VPS, deploy the site via Coolify, and cut over DNS from Framer to the self-hosted VPS. This plan is a human checkpoint -- all actions require access to external dashboards (Hetzner Cloud, Coolify UI, domain registrar).

Purpose: Get the site live at honeylink.nl with automatic HTTPS, replacing the Framer-hosted version.
Output: Live site at https://honeylink.nl served from Hetzner VPS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment/04-CONTEXT.md
@.planning/phases/04-deployment/04-01-SUMMARY.md
@docker-compose.prod.yml
@Caddyfile
@.env.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision VPS, deploy via Coolify, and cut over DNS</name>
  <read_first>
    docker-compose.prod.yml,
    Caddyfile,
    .env.example
  </read_first>
  <action>
This task requires human access to external dashboards. The following steps must be completed by the user.

**Step 1: Hetzner VPS (DEPL-01)**
1. Log in to Hetzner Cloud Console (https://console.hetzner.cloud)
2. Create a new server:
   - Location: Falkenstein (eu-central) or Nuremberg
   - Image: Ubuntu 24.04
   - Type: CPX11 (2 vCPU, 2 GB RAM, 40 GB SSD) -- EUR ~4.50/month
   - SSH key: Add your SSH public key
   - Name: `honeylink-web`
3. Note the server's public IPv4 address

**Step 2: Install Coolify on VPS**
1. SSH into the VPS: `ssh root@<VPS_IP>`
2. Install Coolify:
   ```bash
   curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
   ```
3. Access Coolify UI at `http://<VPS_IP>:8000`
4. Complete initial Coolify setup (create admin account)

**Step 3: Configure Coolify project**
1. In Coolify UI: Add new Resource -> Docker Compose
2. Connect to the GitHub repository containing this project
3. Set the Docker Compose file path to `docker-compose.prod.yml`
4. Set environment variables in Coolify:
   - `RESEND_API_KEY` = your Resend API key from https://resend.com/api-keys
   - `ORIGIN` = `https://honeylink.nl`
   - `PORT` = `3000`
5. Deploy the application
6. Verify the app is running: `curl http://<VPS_IP>:80` should return HTML

**Step 4: DNS cutover (DEPL-05, per D-16, D-17)**
1. Before cutover: Lower DNS TTL to 300 seconds (5 minutes) at your domain registrar
2. Wait for old TTL to expire (check current TTL value first)
3. Update DNS records:
   - `A` record for `honeylink.nl` -> `<VPS_IP>`
   - `A` record for `www.honeylink.nl` -> `<VPS_IP>`
   - Remove any Framer-specific DNS records (CNAME, etc.)
4. Wait for DNS propagation (check with `dig honeylink.nl` or https://dnschecker.org)
5. Verify: `curl -I https://honeylink.nl` shows 200 with valid TLS certificate

**Step 5: Verify production site**
1. Visit https://honeylink.nl -- homepage loads
2. Visit https://honeylink.nl/blogs -- blog listing loads
3. Visit https://honeylink.nl/contact -- contact form visible
4. Submit a test contact form message -- email arrives
5. Check a few other pages: /automation, /cases, /over-ons
6. Verify HTTPS certificate is valid (browser shows lock icon)
  </action>
  <how-to-verify>
    1. Open https://honeylink.nl in a browser -- site loads with lock icon (valid HTTPS)
    2. Navigate to /blogs, /cases, /contact, /automation, /over-ons -- all pages render
    3. Submit a test message via /contact -- confirm email delivery
    4. Run `curl -I https://honeylink.nl` -- returns HTTP/2 200 with security headers
    5. Check all 36 URLs from the Framer site resolve (no 404s)
  </how-to-verify>
  <resume-signal>Type "deployed" when the site is live at honeylink.nl, or describe any issues encountered</resume-signal>
  <acceptance_criteria>
    - `curl -I https://honeylink.nl` returns HTTP 200 status
    - `curl -I https://honeylink.nl` shows `strict-transport-security` header
    - `curl https://honeylink.nl/blogs` returns HTML with blog listing content
    - `curl https://honeylink.nl/contact` returns HTML with contact form
    - DNS A record for honeylink.nl points to the Hetzner VPS IP (not Framer)
    - Contact form submission delivers email via Resend
  </acceptance_criteria>
  <done>The site is live at https://honeylink.nl on the Hetzner VPS with valid HTTPS certificate, Coolify managing deployments, Docker restart policy for crash recovery, and DNS pointing to the new server.</done>
</task>

</tasks>

<verification>
After deployment:
1. `curl -sI https://honeylink.nl | grep -i "http/2 200"` returns match
2. `curl -sI https://honeylink.nl | grep -i "strict-transport-security"` returns match
3. `dig honeylink.nl +short` returns the Hetzner VPS IP
4. All 36 existing Framer URLs return 200 (spot-check 5-10)
</verification>

<success_criteria>
- https://honeylink.nl serves the SvelteKit site with valid TLS
- Coolify handles automatic deployments on git push
- Docker restart policy provides crash recovery
- DNS is migrated from Framer to Hetzner VPS
- All existing URLs work without 404s
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment/04-02-SUMMARY.md`
</output>
