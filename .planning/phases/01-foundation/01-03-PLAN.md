---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/routes/+page.svelte
  - src/lib/components/homepage/HeroSection.svelte
  - src/lib/components/homepage/ServicesSection.svelte
  - src/lib/components/homepage/CasesSection.svelte
  - src/lib/components/homepage/TestimonialsSection.svelte
  - src/lib/components/homepage/BlogSection.svelte
  - src/lib/components/homepage/CtaSection.svelte
  - src/lib/components/homepage/ClientsSection.svelte
autonomous: false
requirements:
  - FOUN-04

must_haves:
  truths:
    - "Visiting / renders the homepage with all sections matching the current honeylink.nl homepage"
    - "Hero section shows headline, subtitle, and CTA button linking to /contact"
    - "Services section displays service cards in a responsive grid"
    - "Testimonials section shows client quotes"
    - "Page sections fade in on scroll"
    - "Layout adapts correctly between mobile, tablet, and desktop"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Homepage composed of section components"
      min_lines: 20
    - path: "src/lib/components/homepage/HeroSection.svelte"
      provides: "Hero with headline, subtitle, CTA"
      min_lines: 20
      contains: "/contact"
    - path: "src/lib/components/homepage/ServicesSection.svelte"
      provides: "Services overview cards"
      min_lines: 20
    - path: "src/lib/components/homepage/TestimonialsSection.svelte"
      provides: "Client testimonials"
      min_lines: 15
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/homepage/HeroSection.svelte"
      via: "component import"
      pattern: "import HeroSection"
    - from: "src/lib/components/homepage/HeroSection.svelte"
      to: "/contact"
      via: "CTA button href"
      pattern: "href.*contact"
    - from: "src/lib/components/homepage/*.svelte"
      to: "src/lib/actions/inview.ts"
      via: "use:inview directive for scroll animations"
      pattern: "use:inview"
---

<objective>
Build the homepage with all sections matching the current honeylink.nl, using scraped content and screenshots as reference. Hero section modernized per D-09.

Purpose: Completes FOUN-04 -- the homepage is the primary landing page and must faithfully represent HoneyLink's brand and services. This is the most visible page and sets the visual standard for all other pages.
Output: Homepage at / with Hero, Clients, Services, Cases, Testimonials, Blog preview, and CTA sections -- all responsive and with scroll fade-in animations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Components and utilities from Plans 01 and 02 -->

From src/lib/actions/inview.ts:
```typescript
export function inview(node: HTMLElement, options?: { threshold?: number; once?: boolean }): { destroy(): void };
```
Usage: `<section use:inview class="opacity-0 translate-y-4 transition-all duration-700 ease-out [&.in-view]:opacity-100 [&.in-view]:translate-y-0">`

From src/lib/components/ui/Button.svelte:
```svelte
<!-- Props: href, variant ('primary'|'secondary'|'outline'), size ('sm'|'md'|'lg'), children snippet -->
<Button href="/contact" variant="primary" size="lg">Neem contact op</Button>
```

From src/lib/types/index.ts:
```typescript
export interface NavLink { label: string; href: string; children?: NavLink[]; }
export interface SiteConfig { name: string; url: string; description: string; }
```

Brand colors from src/app.css @theme:
- brand-purple: #5f09de, brand-gold: #fcc00a, brand-black: #000000
- brand-gray-light: #f6f7f9, brand-gray-dark: #3b3b3b, brand-blue: #0099ff
- Pastels: accent-purple, accent-pink, accent-teal, accent-yellow, accent-blue, accent-green

Font classes: font-sans (Inter), font-heading (Satoshi), font-accent (Geist), font-mono (Fragment Mono)
</interfaces>

<!-- Scraped content for homepage -->
@scripts/scraped-data/content/home.json
@scripts/scraped-data/screenshots/home.png
@scripts/scraped-data/screenshots/home--mobile.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build all homepage section components and compose them in +page.svelte</name>
  <files>
    src/lib/components/homepage/HeroSection.svelte,
    src/lib/components/homepage/ServicesSection.svelte,
    src/lib/components/homepage/CasesSection.svelte,
    src/lib/components/homepage/TestimonialsSection.svelte,
    src/lib/components/homepage/BlogSection.svelte,
    src/lib/components/homepage/CtaSection.svelte,
    src/lib/components/homepage/ClientsSection.svelte,
    src/routes/+page.svelte
  </files>
  <read_first>
    scripts/scraped-data/content/home.json (ALL section content -- headings, paragraphs, images, links. This is the primary source of truth for text content),
    scripts/scraped-data/screenshots/home.png (visual reference for section order, spacing, colors, layout -- compare side-by-side while building),
    scripts/scraped-data/screenshots/home--mobile.png (mobile layout reference),
    .planning/phases/01-foundation/01-CONTEXT.md (D-09 modernized hero, D-10 same section order, D-11 CTAs link to /contact, D-07 fade-in animations),
    .planning/phases/01-foundation/01-RESEARCH.md (Homepage Structure section, Color Palette, Typography Plan, Pattern 3 IntersectionObserver, Responsive Breakpoints),
    src/app.css (brand colors and fonts for Tailwind class reference),
    src/lib/actions/inview.ts (inview action API),
    src/lib/components/ui/Button.svelte (Button component props),
    CLAUDE.md (Svelte 5 syntax: $props, $state, onclick, {@render children()}, no bind:)
  </read_first>
  <action>
    Read `scripts/scraped-data/content/home.json` and `scripts/scraped-data/screenshots/home.png` carefully. The homepage sections should follow the EXACT order observed on honeylink.nl (per D-10). The research doc suggests this order:

    1. Hero Section
    2. Client/trust indicators
    3. Services overview
    4. Case studies preview
    5. Testimonials
    6. Blog preview
    7. Final CTA

    Adjust the order if the scraped data reveals a different structure. Create one component per section.

    **HeroSection.svelte** (per D-09 -- modernized layout, same content):

    - Use scraped hero headline text (from home.json headings[0] or first h1)
    - Use scraped subtitle text
    - CTA button links to /contact (per D-11): `<Button href="/contact" variant="primary" size="lg">{scraped CTA label}</Button>`
    - Gold radial gradient background (per research Homepage Structure): use `bg-gradient-radial from-brand-gold/20 to-transparent` or a custom radial gradient via inline style
    - Layout: centered text with generous vertical padding
    - Modernized sizing (per D-09): larger headline (text-4xl md:text-5xl lg:text-7xl), wider max-width, more breathing room
    - Font: font-heading for headline (Satoshi), font-sans for subtitle (Inter)
    - Do NOT add `use:inview` to hero -- it should be immediately visible (above the fold)

    ```svelte
    <script lang="ts">
      import Button from '$lib/components/ui/Button.svelte';
    </script>

    <section class="relative overflow-hidden px-4 py-20 md:py-28 lg:py-36">
      <!-- Gold radial gradient background -->
      <div class="absolute inset-0 bg-gradient-to-br from-brand-gold/10 via-transparent to-brand-purple/5"></div>

      <div class="relative mx-auto max-w-4xl text-center">
        <h1 class="font-heading text-4xl md:text-5xl lg:text-7xl font-bold leading-tight mb-6">
          <!-- SCRAPED HEADLINE TEXT HERE (Dutch) -->
        </h1>
        <p class="text-lg md:text-xl text-brand-gray-dark max-w-2xl mx-auto mb-10">
          <!-- SCRAPED SUBTITLE TEXT HERE (Dutch) -->
        </p>
        <Button href="/contact" variant="primary" size="lg">
          <!-- SCRAPED CTA LABEL or "Neem contact op" -->
        </Button>
      </div>
    </section>
    ```

    **ClientsSection.svelte** (trust indicators / client logos):

    - Display client logos or trust badges from scraped images
    - Horizontal row with grayscale filter, auto-spacing
    - Use `use:inview` for fade-in animation

    **ServicesSection.svelte** (per D-10):

    - Card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
    - Each card uses one of the pastel accent colors (accent-purple, accent-pink, accent-teal, etc.) as background
    - Service name as card heading (font-heading), short description, link to service page
    - Content from scraped data (services listed on homepage)
    - Use `use:inview` for fade-in

    **CasesSection.svelte** (case studies preview):

    - Show 2-3 featured case studies from scraped homepage content
    - Card layout with image, client name, short description
    - Link to /cases and individual case pages
    - Use `use:inview` for fade-in

    **TestimonialsSection.svelte**:

    - Client quotes from scraped data
    - Card or blockquote layout with quote text, client name, company
    - Use `use:inview` for fade-in

    **BlogSection.svelte** (recent posts preview):

    - Show 3 recent blog post cards from scraped homepage content
    - Card with title, excerpt, date
    - Link to /blogs and individual blog pages
    - Use `use:inview` for fade-in

    **CtaSection.svelte** (final call-to-action):

    - Full-width section with compelling headline and CTA button (per D-11, links to /contact)
    - Dark or colored background for contrast
    - Use `use:inview` for fade-in

    **CRITICAL for all section components:**
    - ALL text content MUST come from the scraped data (home.json). Do NOT write placeholder/generic text. Use the actual Dutch text from the Framer site.
    - If a section's content is unclear from scraped data, use the screenshot as visual reference and match as closely as possible.
    - Every section below the fold MUST use `use:inview` with the fade-in classes:
      ```
      use:inview class="opacity-0 translate-y-4 transition-all duration-700 ease-out [&.in-view]:opacity-100 [&.in-view]:translate-y-0"
      ```
    - All sections use responsive classes: single column on mobile, multi-column on md: and lg:
    - Use `mx-auto max-w-7xl px-4` for consistent content width across sections
    - Images use scraped images from `static/images/` (or `/images/` paths). If images are missing, use a colored placeholder div.

    **Compose in `src/routes/+page.svelte`:**

    ```svelte
    <script lang="ts">
      import HeroSection from '$lib/components/homepage/HeroSection.svelte';
      import ClientsSection from '$lib/components/homepage/ClientsSection.svelte';
      import ServicesSection from '$lib/components/homepage/ServicesSection.svelte';
      import CasesSection from '$lib/components/homepage/CasesSection.svelte';
      import TestimonialsSection from '$lib/components/homepage/TestimonialsSection.svelte';
      import BlogSection from '$lib/components/homepage/BlogSection.svelte';
      import CtaSection from '$lib/components/homepage/CtaSection.svelte';
    </script>

    <HeroSection />
    <ClientsSection />
    <ServicesSection />
    <CasesSection />
    <TestimonialsSection />
    <BlogSection />
    <CtaSection />
    ```

    Adjust the component order to match what the scraped screenshot reveals. If the Framer site does not have one of these sections (e.g., no ClientsSection), omit it.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/+page.svelte imports at least 5 section components
    - src/routes/+page.svelte does NOT contain placeholder text like "coming in Plan"
    - src/lib/components/homepage/HeroSection.svelte contains "/contact" (CTA link per D-11)
    - src/lib/components/homepage/HeroSection.svelte contains "font-heading" (Satoshi for headline)
    - src/lib/components/homepage/HeroSection.svelte contains text-4xl or text-5xl or text-7xl (large headline)
    - src/lib/components/homepage/ServicesSection.svelte contains "use:inview" (scroll animation per D-07)
    - src/lib/components/homepage/ServicesSection.svelte contains "grid" (responsive grid layout)
    - src/lib/components/homepage/TestimonialsSection.svelte contains "use:inview"
    - src/lib/components/homepage/CtaSection.svelte contains "/contact" (final CTA per D-11)
    - All homepage section .svelte files use $props() (if receiving props) and NOT "export let"
    - No homepage section file contains "bind:" or "<slot" or "on:" event syntax
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Homepage renders all sections in order matching honeylink.nl. Hero shows headline + CTA. Services show card grid. Testimonials show quotes. All sections below fold fade in on scroll. All CTAs link to /contact. Layout is responsive across mobile/tablet/desktop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of homepage against Framer site</name>
  <files>src/routes/+page.svelte, src/lib/components/homepage/HeroSection.svelte</files>
  <action>
    Human verification checkpoint. Present the running dev server to the user for visual comparison against the Framer site screenshots.

    What was built: Complete homepage with Hero, Client logos, Services grid, Case studies preview, Testimonials, Blog preview, and CTA sections. All content sourced from scraped Framer data. Responsive design with scroll animations.

    Steps for user to verify:
    1. Run `npm run dev` and open http://localhost:5173 in a browser.
    2. Side-by-side comparison: Open scripts/scraped-data/screenshots/home.png alongside the dev server. Compare section order, heading text, and overall layout. Colors should match (purple #5f09de, gold #fcc00a). Fonts should be Satoshi for headings, Inter for body text.
    3. Hero section (per D-09): Headline text matches Framer (in Dutch). CTA button is visible and links to /contact. Layout feels modern -- sizing should be improved over Framer, not a downgrade.
    4. Scroll behavior (per D-07): Scroll down slowly -- sections should fade in as they enter the viewport. Animation should be subtle, not distracting.
    5. Responsive check: Resize browser to mobile width (375px). Compare with scripts/scraped-data/screenshots/home--mobile.png. All sections should stack vertically. Cards should be single column. Text should be readable without horizontal scroll.
    6. Content accuracy: All text is in Dutch (not English or placeholder). Service names match the Framer site. Testimonial quotes match the Framer site. No "Lorem ipsum" or "TODO" text visible.
    7. Navigation (from Plan 02): Header is sticky while scrolling. Footer is at the bottom with correct links.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has approved that the homepage visually matches the Framer site design. Type "approved" if homepage matches, or describe specific issues to fix.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. `grep -r "use:inview" src/lib/components/homepage/` returns matches in multiple files
3. `grep -r "/contact" src/lib/components/homepage/HeroSection.svelte` matches
4. `grep -r "/contact" src/lib/components/homepage/CtaSection.svelte` matches
5. Homepage at http://localhost:5173/ renders all sections without errors
6. Browser console shows no errors on homepage load
</verification>

<success_criteria>
- Homepage at / renders all sections matching honeylink.nl section order and content
- Hero section has modernized layout with headline, subtitle, and /contact CTA
- Services section shows cards in responsive grid
- All sections below fold use scroll-triggered fade-in animations
- All CTA buttons link to /contact
- Content is in Dutch and matches the scraped Framer site content
- Layout is responsive: single column mobile, multi-column tablet/desktop
- npm run build succeeds
- User approves visual match with Framer site via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
