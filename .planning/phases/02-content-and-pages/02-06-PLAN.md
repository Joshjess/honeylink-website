---
phase: 02-content-and-pages
plan: 06
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04", "02-05"]
files_modified: []
autonomous: false
requirements:
  - CONT-02
  - CONT-03
  - CONT-06
  - CONT-07
  - SERV-01
  - STAT-01

must_haves:
  truths:
    - "All pages render correctly in the browser without console errors"
    - "Navigation to every content page works from the listing pages"
    - "Responsive layout works on mobile and desktop"
    - "Content matches the original Framer site"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all content pages created in Plans 02-05. Human confirms that blog listing, blog posts, case listing, case studies, all 7 service pages, about page, and 3 legal pages render correctly and match the Framer originals.

Purpose: Catch visual issues, broken images, missing content, or layout problems before proceeding to Phase 3. This is the quality gate for all Phase 2 content.

Output: Human approval or list of issues to fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-and-pages/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build verification and start dev server</name>
  <files>package.json (read-only -- no files modified, build/check verification only)</files>
  <read_first>
    package.json
  </read_first>
  <action>
    1. Run `npm run build` to verify the entire site builds without errors
    2. Run `npm run check` to verify TypeScript passes
    3. Start the dev server with `npm run dev` in the background
    4. Report the dev server URL (typically http://localhost:5173)
    5. List all routes that should be tested:
       - /blogs (listing page)
       - /blogs/waarom-ben-ik-honeylink-begonnen (sample blog post)
       - /blogs/krijg-weer-grip-op-je-data-met-model-context-protocol-(mcp) (slug with parentheses)
       - /cases (listing page)
       - /cases/van-volgers-naar-kunstenaars-novus-arte-s-geautomatiseerde-talentscouting (sample case)
       - /automation (service page)
       - /data-verrijking (service page)
       - /api (service page)
       - /maatwerk-software (service page)
       - /offerte-automatisering (service page)
       - /ai-agent (service page)
       - /chatbot (service page)
       - /over-ons (about page)
       - /terms-conditions (legal page)
       - /betalings-voorwaarden (legal page)
       - /privacy-policy (legal page)
  </action>
  <verify>
    <automated>cd /home/joshua/HoneyLink/website && npm run build 2>&1 | tail -5 && echo "BUILD OK" && npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `npm run check` exits with code 0
    - Dev server starts successfully
  </acceptance_criteria>
  <done>Build and type-check pass, dev server running, all routes listed for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all content pages</name>
  <action>
    Present the user with the verification checklist below. The user visits each page in the browser and compares against Framer screenshots in scripts/scraped-data/screenshots/.

    **Blog system:**
    1. Visit http://localhost:5173/blogs -- verify grid shows all 16 posts with images, dates, titles, excerpts
    2. Click any blog post -- verify it renders with date (purple text), title, hero image, author with avatar, reading time ("N min leestijd"), prose content, and CTA section
    3. Visit http://localhost:5173/blogs/krijg-weer-grip-op-je-data-met-model-context-protocol-(mcp) -- verify slug with parentheses works

    **Case studies:**
    4. Visit http://localhost:5173/cases -- verify grid shows all 5 cases with images and titles
    5. Click any case -- verify it renders with title, gold-background hero image, author info (NO reading time), prose content, and CTA section

    **Service pages (check at least 2):**
    6. Visit http://localhost:5173/automation -- verify gold hero, value proposition, feature cards, practical examples, mini CTA ("Nieuwsgierig?"), testimonials, FAQ accordion, CTA section
    7. Visit http://localhost:5173/chatbot -- verify same pattern with different content
    8. Click FAQ accordion items -- verify they open/close smoothly

    **About page:**
    9. Visit http://localhost:5173/over-ons -- verify mission section, team photos, FAQ, CTA

    **Legal pages:**
    10. Visit http://localhost:5173/privacy-policy -- verify prose content renders, NO CTA section at bottom
    11. Visit http://localhost:5173/terms-conditions -- verify prose content renders

    **Responsive:**
    12. Resize browser to mobile width (~375px) -- verify single-column layouts, no horizontal overflow

    **Console:**
    13. Check browser console -- no JavaScript errors, no 404s for images
  </action>
  <verify>User confirms visual verification passes by typing "approved" or describes issues</verify>
  <acceptance_criteria>
    - User confirms all pages render correctly
    - No broken images reported
    - No console errors reported
    - Responsive layout confirmed working
  </acceptance_criteria>
  <done>Human approves all content pages or provides specific feedback for fixes.</done>
</task>

</tasks>

<verification>
- Build succeeds with zero errors
- All 17 content routes render in the browser
- Content matches Framer originals
- No broken images or console errors
- Responsive layout works
</verification>

<success_criteria>
- Human approves that all pages render correctly
- Or: issues identified and documented for gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-and-pages/02-06-SUMMARY.md`
</output>
