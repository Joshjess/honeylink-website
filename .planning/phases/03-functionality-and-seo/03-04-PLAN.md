---
phase: 03-functionality-and-seo
plan: 04
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/routes/+page.svelte
  - src/routes/blogs/[slug]/+page.svelte
  - src/routes/cases/[slug]/+page.svelte
  - src/routes/automation/+page.svelte
  - src/routes/data-verrijking/+page.svelte
  - src/routes/api/+page.svelte
  - src/routes/maatwerk-software/+page.svelte
  - src/routes/offerte-automatisering/+page.svelte
  - src/routes/ai-agent/+page.svelte
  - src/routes/chatbot/+page.svelte
  - src/routes/blogs/+page.svelte
  - src/routes/cases/+page.svelte
  - src/routes/over-ons/+page.svelte
  - src/routes/contact/+page.svelte
  - src/routes/terms-conditions/+page.svelte
  - src/routes/betalings-voorwaarden/+page.svelte
  - src/routes/privacy-policy/+page.svelte
autonomous: false
requirements: [SEO-06, SEO-08]

must_haves:
  truths:
    - "Homepage page source contains a <script type='application/ld+json'> with Organization schema"
    - "Blog post page source contains a <script type='application/ld+json'> with Article schema"
    - "Service page source contains a <script type='application/ld+json'> with Service schema"
    - "All inner pages have a BreadcrumbList JSON-LD in page source"
    - "No duplicate meta tags in page source (single title, single description)"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Organization JSON-LD on homepage"
      contains: "JsonLd"
    - path: "src/routes/blogs/[slug]/+page.svelte"
      provides: "Article JSON-LD on blog posts"
      contains: "JsonLd"
    - path: "src/routes/automation/+page.svelte"
      provides: "Service + BreadcrumbList JSON-LD on service pages"
      contains: "JsonLd"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "svelte-meta-tags JsonLd component"
      via: "JsonLd schema prop with Organization type"
      pattern: "@type.*Organization"
    - from: "src/routes/blogs/[slug]/+page.svelte"
      to: "svelte-meta-tags JsonLd component"
      via: "JsonLd schema prop with Article type"
      pattern: "@type.*Article"
---

<objective>
Add JSON-LD structured data to all pages using svelte-meta-tags JsonLd component: Organization schema on homepage, Article schema on blog posts, Service schema on service pages, and BreadcrumbList on all inner pages. Then verify the complete Phase 3 output visually.

Purpose: Structured data enables rich results in Google search (knowledge panels, article cards, breadcrumb trails). This is the final SEO layer making the site production-ready for search engines. Depends on Plan 02 having MetaTags and svelte-meta-tags installed.
Output: JSON-LD structured data on every page type, visual verification of complete Phase 3.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-functionality-and-seo/03-CONTEXT.md
@.planning/phases/03-functionality-and-seo/03-RESEARCH.md
@.planning/phases/03-functionality-and-seo/03-02-SUMMARY.md

@src/routes/+page.svelte
@src/routes/+layout.svelte
@src/routes/blogs/[slug]/+page.svelte
@src/routes/cases/[slug]/+page.svelte
@src/routes/automation/+page.svelte

<interfaces>
From svelte-meta-tags (JsonLd component):
```svelte
<script lang="ts">
  import { JsonLd } from 'svelte-meta-tags';
</script>

<JsonLd schema={{
  '@type': 'Organization',
  name: '...',
  url: '...'
}} />
```

Blog post page data (from +page.ts load):
```typescript
// data.meta contains: { title, slug, date, author, authorImage, excerpt, image, published, readingTime }
// data.seo contains: { title, description, image, type }
```

Case study page data (from +page.ts load):
```typescript
// data.meta contains: { title, slug, client, industry, author, authorImage, excerpt, image, published }
// data.seo contains: { title, description, image, type }
```

Service page pattern (imports pageData directly):
```svelte
import { pageData } from '$lib/data/services/automation';
// pageData contains: { title, subtitle, sections, faq }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSON-LD structured data (Organization, Article, Service, BreadcrumbList) to all page types</name>
  <files>
    src/routes/+page.svelte,
    src/routes/blogs/[slug]/+page.svelte,
    src/routes/cases/[slug]/+page.svelte,
    src/routes/automation/+page.svelte,
    src/routes/data-verrijking/+page.svelte,
    src/routes/api/+page.svelte,
    src/routes/maatwerk-software/+page.svelte,
    src/routes/offerte-automatisering/+page.svelte,
    src/routes/ai-agent/+page.svelte,
    src/routes/chatbot/+page.svelte,
    src/routes/blogs/+page.svelte,
    src/routes/cases/+page.svelte,
    src/routes/over-ons/+page.svelte,
    src/routes/contact/+page.svelte,
    src/routes/terms-conditions/+page.svelte,
    src/routes/betalings-voorwaarden/+page.svelte,
    src/routes/privacy-policy/+page.svelte
  </files>
  <read_first>
    src/routes/+page.svelte,
    src/routes/blogs/[slug]/+page.svelte,
    src/routes/cases/[slug]/+page.svelte,
    src/routes/automation/+page.svelte,
    src/routes/data-verrijking/+page.svelte,
    src/routes/blogs/+page.svelte,
    src/routes/cases/+page.svelte,
    src/routes/over-ons/+page.svelte,
    src/routes/contact/+page.svelte,
    src/routes/terms-conditions/+page.svelte,
    src/lib/data/services/automation.ts,
    .planning/phases/03-functionality-and-seo/03-CONTEXT.md
  </read_first>
  <action>
Add `import { JsonLd } from 'svelte-meta-tags';` to each page that needs structured data. Add the JsonLd component(s) at the top of the template (before visible HTML). Do NOT add MetaTags (that is already in layout per Plan 02). Only add JsonLd components.

**1. Homepage (`src/routes/+page.svelte`) -- Organization schema per D-17:**

Add to the script section:
```typescript
import { JsonLd } from 'svelte-meta-tags';
```

Add before the first visible element:
```svelte
<JsonLd schema={{
  '@type': 'Organization',
  name: 'HoneyLink',
  url: 'https://honeylink.nl',
  logo: 'https://honeylink.nl/images/logo.png',
  contactPoint: {
    '@type': 'ContactPoint',
    telephone: '+31-20-308-68-40',
    email: 'info@honeylink.nl',
    contactType: 'customer service'
  },
  address: {
    '@type': 'PostalAddress',
    streetAddress: 'Marco Polostraat 275-3',
    addressLocality: 'Amsterdam',
    postalCode: '1056DN',
    addressCountry: 'NL'
  }
}} />
```

**2. Blog post pages (`src/routes/blogs/[slug]/+page.svelte`) -- Article schema per D-18 + BreadcrumbList per D-20:**

Add import and:
```svelte
<JsonLd schema={{
  '@type': 'Article',
  headline: data.meta.title,
  author: { '@type': 'Person', name: data.meta.author },
  datePublished: data.meta.date,
  image: `https://honeylink.nl${data.meta.image}`,
  publisher: {
    '@type': 'Organization',
    name: 'HoneyLink',
    logo: { '@type': 'ImageObject', url: 'https://honeylink.nl/images/logo.png' }
  }
}} />

<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Blogs', item: 'https://honeylink.nl/blogs' },
    { '@type': 'ListItem', position: 3, name: data.meta.title }
  ]
}} />
```
Access `data` via `let { data } = $props()` -- check if the page already destructures data or uses a different pattern and adapt accordingly.

**3. Case study pages (`src/routes/cases/[slug]/+page.svelte`) -- Article schema + BreadcrumbList per D-20:**

```svelte
<JsonLd schema={{
  '@type': 'Article',
  headline: data.meta.title,
  author: { '@type': 'Person', name: data.meta.author },
  image: `https://honeylink.nl${data.meta.image}`,
  publisher: {
    '@type': 'Organization',
    name: 'HoneyLink',
    logo: { '@type': 'ImageObject', url: 'https://honeylink.nl/images/logo.png' }
  }
}} />

<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Cases', item: 'https://honeylink.nl/cases' },
    { '@type': 'ListItem', position: 3, name: data.meta.title }
  ]
}} />
```

**4. All 7 service pages -- Service schema per D-19 + BreadcrumbList per D-20:**

For each service page (`src/routes/automation/+page.svelte`, `src/routes/data-verrijking/+page.svelte`, `src/routes/api/+page.svelte`, `src/routes/maatwerk-software/+page.svelte`, `src/routes/offerte-automatisering/+page.svelte`, `src/routes/ai-agent/+page.svelte`, `src/routes/chatbot/+page.svelte`):

Add `import { JsonLd } from 'svelte-meta-tags';` to the existing script imports.

Add before the first visible element (each page already imports `pageData` which has `title` and `subtitle`):
```svelte
<JsonLd schema={{
  '@type': 'Service',
  name: pageData.title,
  description: pageData.subtitle,
  provider: {
    '@type': 'Organization',
    name: 'HoneyLink',
    url: 'https://honeylink.nl'
  }
}} />

<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: pageData.title }
  ]
}} />
```

**5. Remaining inner pages -- BreadcrumbList per D-20:**

For `src/routes/blogs/+page.svelte` (blog listing):
```svelte
<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Blogs' }
  ]
}} />
```

For `src/routes/cases/+page.svelte` (case listing):
```svelte
<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Cases' }
  ]
}} />
```

For `src/routes/over-ons/+page.svelte`:
```svelte
<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Over Ons' }
  ]
}} />
```

For `src/routes/contact/+page.svelte`:
```svelte
<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Contact' }
  ]
}} />
```

For `src/routes/terms-conditions/+page.svelte`:
```svelte
<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Algemene Voorwaarden' }
  ]
}} />
```

For `src/routes/betalings-voorwaarden/+page.svelte`:
```svelte
<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Betalingsvoorwaarden' }
  ]
}} />
```

For `src/routes/privacy-policy/+page.svelte`:
```svelte
<JsonLd schema={{
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://honeylink.nl' },
    { '@type': 'ListItem', position: 2, name: 'Privacy Policy' }
  ]
}} />
```

**Pattern reminder:** Each page adds `import { JsonLd } from 'svelte-meta-tags';` to the `<script>` block and places the `<JsonLd>` component(s) at the top of the template before any visible HTML. Do NOT add MetaTags to any page -- that is already handled by the root layout (Plan 02).
  </action>
  <verify>
    <automated>cd /home/joshua/HoneyLink/website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/+page.svelte contains "import { JsonLd } from 'svelte-meta-tags'"
    - src/routes/+page.svelte contains "'@type': 'Organization'"
    - src/routes/+page.svelte contains "HoneyLink" in Organization schema
    - src/routes/+page.svelte contains "Marco Polostraat 275-3"
    - src/routes/blogs/[slug]/+page.svelte contains "import { JsonLd } from 'svelte-meta-tags'"
    - src/routes/blogs/[slug]/+page.svelte contains "'@type': 'Article'"
    - src/routes/blogs/[slug]/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/cases/[slug]/+page.svelte contains "'@type': 'Article'"
    - src/routes/cases/[slug]/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/automation/+page.svelte contains "'@type': 'Service'"
    - src/routes/automation/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/data-verrijking/+page.svelte contains "'@type': 'Service'"
    - src/routes/api/+page.svelte contains "'@type': 'Service'"
    - src/routes/maatwerk-software/+page.svelte contains "'@type': 'Service'"
    - src/routes/offerte-automatisering/+page.svelte contains "'@type': 'Service'"
    - src/routes/ai-agent/+page.svelte contains "'@type': 'Service'"
    - src/routes/chatbot/+page.svelte contains "'@type': 'Service'"
    - src/routes/over-ons/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/contact/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/blogs/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/cases/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/terms-conditions/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/betalings-voorwaarden/+page.svelte contains "'@type': 'BreadcrumbList'"
    - src/routes/privacy-policy/+page.svelte contains "'@type': 'BreadcrumbList'"
    - npm run build exits 0
  </acceptance_criteria>
  <done>JSON-LD structured data on all pages: Organization on homepage, Article on blog/case posts, Service on 7 service pages, BreadcrumbList on all inner pages. All schemas use absolute URLs. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete Phase 3</name>
  <files>none (verification only)</files>
  <action>
Human verifies the complete Phase 3 output across all 4 plans: contact form, SEO meta tags, JSON-LD structured data, sitemap, robots.txt, security headers, and image optimization. Start the dev server with `npm run dev` and walk through the verification checklist below.

**What was built across Phase 3:**
- Plan 01: Contact form with Superforms + Zod 4 validation, Resend email delivery, rate limiting, FAQ section
- Plan 02: MetaTags on every page via root layout, page-specific SEO data in load functions, sitemap.xml, robots.txt
- Plan 03: CSP and security headers in hooks.server.ts, enhanced-img on 5 component images
- Plan 04 Task 1: JSON-LD structured data (Organization, Article, Service, BreadcrumbList) on all pages
  </action>
  <verify>
    <automated>cd /home/joshua/HoneyLink/website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <what-built>
    Complete Phase 3: contact form with email delivery, SEO meta tags on all pages, JSON-LD structured data, sitemap.xml, robots.txt, security headers, and image optimization.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:5173/contact
       - Verify two-column layout: left side has heading, description, contact details; right side has form
       - Verify form has 4 fields: Naam, E-mail, Bedrijf (optioneel), Bericht
       - Verify submit button says "Verstuur bericht"
       - Try submitting empty form -- should show Dutch validation errors
       - Verify FAQ section below form with 5 questions
       - On mobile viewport (< 1024px): single column layout

    2. View page source (Ctrl+U) on any page:
       - Check for `<title>Page Name | HoneyLink</title>` format
       - Check for `<meta name="description"` with Dutch content
       - Check for `og:title`, `og:description`, `og:image`, `og:url` meta tags
       - Check for `<link rel="canonical"` with absolute URL
       - Check for `<script type="application/ld+json">` with appropriate schema

    3. Visit http://localhost:5173/sitemap.xml
       - Should return XML with all routes listed
       - Should include /blogs/[slug] entries for all 16 blog posts
       - Should include /cases/[slug] entries for all 5 case studies

    4. Check response headers (browser DevTools -> Network -> any page -> Headers):
       - Content-Security-Policy header present
       - X-Frame-Options: DENY
       - X-Content-Type-Options: nosniff

    5. Verify images load correctly on homepage (no broken images after enhanced:img migration)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <acceptance_criteria>
    - Human confirms contact form renders with two-column layout and all 4 fields
    - Human confirms validation errors appear in Dutch when submitting empty form
    - Human confirms page source shows meta tags, OG tags, canonical URL, and JSON-LD
    - Human confirms /sitemap.xml returns valid XML with all routes
    - Human confirms security headers present in response headers
    - Human confirms images load correctly on homepage
  </acceptance_criteria>
  <done>Human has verified all Phase 3 functionality: contact form layout and validation, SEO meta tags and structured data, sitemap, security headers, and image optimization. Phase 3 is complete and production-ready.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- Every page has JSON-LD structured data appropriate to its type
- No duplicate MetaTags (only in layout)
- Homepage has Organization schema
- Blog posts have Article + BreadcrumbList schemas
- Service pages have Service + BreadcrumbList schemas
- All inner pages have BreadcrumbList schema
- Visual verification confirms contact form, SEO, security, and images all work
</verification>

<success_criteria>
1. Homepage page source contains Organization JSON-LD with HoneyLink details
2. Blog post pages contain Article JSON-LD with headline, author, datePublished
3. Service pages contain Service JSON-LD with name, description, provider
4. All inner pages contain BreadcrumbList JSON-LD
5. No duplicate meta tags or JSON-LD blocks
6. Human approves visual and functional verification of complete Phase 3
7. Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-functionality-and-seo/03-04-SUMMARY.md`
</output>
